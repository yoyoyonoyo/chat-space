.side-bar
  .leftHeader
    .leftHeader__items
      .leftHeader__userName
        = current_user.name
      %ul.leftHeader__icons
        %li<
          = link_to new_group_path, class: "leftHeader__icon" do
            = icon('far', 'edit',class: "leftHeader__iconEdit")
        %li<
          = link_to edit_user_path(current_user), class: "leftHeader__icon" do
            = icon('fas', 'cog',class: "leftHeader__iconCog" )
    
  .leftBar
    - current_user.groups.each do |group|
      .leftBar__items
      = link_to "#" do
        .leftBar__groupName
          = group.name
        .leftBar__groupInfo
          まだメッセージがありません